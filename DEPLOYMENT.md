# 部署说明

这个项目部署到 [ShioLilia.github.io](https://github.com/ShioLilia/ShioLilia.github.io) 仓库的 `src/sonnetaw/` 目录下。

## 💾 空间优化策略

为了节省托管库空间，采用了以下优化：

1. **源代码不上传** - 只上传编译压缩后的 JavaScript bundle
2. **数据文件远程加载** - CMU 字典文件从开发仓库动态加载，不打包到托管库
3. **按需加载** - 字典在首次使用时通过 GitHub raw URL 加载

### 空间对比
- ❌ **传统方式**: 托管库包含所有资源 (~500KB+)
- ✅ **优化后**: 托管库仅包含代码 bundle (~50KB)

数据文件（~450KB）仅保留在开发仓库 `sonnetaw`，运行时通过以下 URL 动态加载：
```
https://raw.githubusercontent.com/ShioLilia/sonnetaw/main/data/cmu-dict-sample.json
```

## 🚀 快速部署

### Windows (PowerShell)
```bash
npm run deploy
```

### Linux/Mac (Bash)
```bash
npm run deploy:bash
```

## 📝 部署流程

脚本会自动执行以下步骤：

1. **构建项目** - 运行 `npm run build` 生成生产版本
2. **克隆托管仓库** - 克隆 ShioLilia.github.io 仓库到临时目录
3. **清理旧文件** - 删除 `src/sonnetaw/` 下的旧版本
4. **复制新文件** - 将 `dist/` 目录的内容复制到 `src/sonnetaw/`
5. **提交推送** - 提交更改并推送到托管仓库
6. **清理临时文件** - 删除临时克隆的仓库

## 🌐 访问地址

部署成功后，访问：
**https://ShioLilia.github.io/src/sonnetaw/**

## ⚙️ 配置说明

- **Vite Base Path**: `/src/sonnetaw/` (在 `vite.config.ts` 中配置)
- **目标仓库**: https://github.com/ShioLilia/ShioLilia.github.io
- **部署路径**: `src/sonnetaw/`

## 📋 注意事项

1. 确保你有 ShioLilia.github.io 仓库的推送权限
2. 首次运行可能需要输入 GitHub 凭据
3. 部署脚本会自动处理文件冲突（完全覆盖旧版本）
4. 建议在本地先运行 `npm run preview` 测试构建结果

## 🔧 手动部署

如果自动脚本遇到问题，可以手动部署：

```bash
# 1. 构建项目
npm run build

# 2. 复制构建产物到托管仓库
# 将 dist/ 目录的内容复制到 ShioLilia.github.io/src/sonnetaw/

# 3. 在托管仓库中提交并推送
cd path/to/ShioLilia.github.io
git add src/sonnetaw
git commit -m "Update sonnetaw"
git push origin main
```

## 🛠️ 开发流程建议

```bash
# 开发时
npm run dev          # 启动开发服务器

# 测试构建
npm run build        # 构建生产版本
npm run preview      # 预览生产版本

# 部署到 GitHub Pages
npm run deploy       # 一键部署
```
