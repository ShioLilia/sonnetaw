# 🎯 手动部署图文教程

这是一个完全手动的部署教程，不需要任何自动化脚本。

---

## 📦 准备工作

### 你需要有这两个仓库：

1. ✅ **sonnetaw** - 开发仓库（当前）
2. ✅ **ShioLilia.github.io** - 托管仓库

---

## 🚀 部署步骤（每次更新都做这些）

### Step 1️⃣: 构建项目

在 `sonnetaw` 文件夹打开终端，运行：

```bash
npm run build
```

**结果：** 会在 `sonnetaw/dist/` 生成文件：
```
dist/
├── index.html
├── assets/
│   ├── main-[hash].js
│   └── main-[hash].css
└── vite.svg
```

---

### Step 2️⃣: 打开两个文件夹

**源文件夹：** `sonnetaw/dist/`  
**目标文件夹：** `ShioLilia.github.io/src/sonnetaw/`

💡 如果 `src/sonnetaw/` 不存在，手动创建这个文件夹。

---

### Step 3️⃣: 复制文件

**全选** `dist/` 里的所有内容，**粘贴**到 `ShioLilia.github.io/src/sonnetaw/`

```
复制前：
ShioLilia.github.io/
└── src/
    └── sonnetaw/        （空文件夹或旧文件）

复制后：
ShioLilia.github.io/
└── src/
    └── sonnetaw/
        ├── index.html   ✅
        ├── assets/      ✅
        │   ├── main-xxx.js
        │   └── main-xxx.css
        └── vite.svg     ✅
```

**允许覆盖旧文件。**

---

### Step 4️⃣: 提交到 GitHub

在 `ShioLilia.github.io` 文件夹打开终端，运行：

```bash
git add src/sonnetaw
git commit -m "Update sonnetaw"
git push
```

或者使用 GitHub Desktop / VS Code 提交。

---

### Step 5️⃣: 等待部署

GitHub Pages 通常需要 **1-3 分钟**更新。

访问：**https://ShioLilia.github.io/src/sonnetaw/**

按 `Ctrl + Shift + R` 强制刷新浏览器。

---

## 🛠️ 简化方法（可选）

### 方法 A: 使用辅助脚本（Windows）

1. **首次设置**：编辑 `copy-to-pages.ps1`，修改第 5 行为你的路径：
   ```powershell
   $TARGET_REPO_PATH = "F:\你的路径\ShioLilia.github.io"
   ```

2. **每次使用**：双击 `copy-to-pages.ps1` 或运行：
   ```powershell
   .\copy-to-pages.ps1
   ```

### 方法 B: 创建快捷命令

在 `package.json` 中添加：
```json
"scripts": {
  "copy": "xcopy dist\\* \"F:\\路径\\ShioLilia.github.io\\src\\sonnetaw\\\" /E /Y"
}
```

然后运行：
```bash
npm run build && npm run copy
```

---

## ❓ 常见问题

### Q: 我需要上传 `data/` 文件夹吗？
**A:** ❌ 不需要！字典文件会从 sonnetaw 仓库在线加载，节省 90% 空间。

### Q: 每次都要复制所有文件吗？
**A:** ✅ 是的，这样最安全。每次构建后文件名会变（包含 hash），直接覆盖就好。

### Q: 能自动化吗？
**A:** 可以，但你说想手动操作。如需自动化，参考旧版的 `deploy-to-pages.ps1` 脚本。

### Q: 为什么访问还是旧版本？
**A:** 
1. 等待 1-3 分钟让 GitHub Pages 更新
2. 按 `Ctrl + Shift + R` 清除浏览器缓存
3. 检查是否正确推送到了 `main` 分支

### Q: 能部署到其他位置吗？
**A:** 可以！修改 `vite.config.ts` 中的 `base` 路径，然后重新构建。

---

## 📋 完整流程总结

```
修改代码
    ↓
npm run build
    ↓
复制 dist/* 到 ShioLilia.github.io/src/sonnetaw/
    ↓
git add + commit + push
    ↓
等待 1-3 分钟
    ↓
访问网站，强制刷新
    ↓
✅ 完成！
```

---

## 💡 小贴士

- **开发时**用 `npm run dev`，可以实时预览
- **部署前**用 `npm run preview`，检查构建结果
- **保持习惯**：每次修改 → 构建 → 复制 → 提交
- **版本管理**：可以在 commit 信息中加上版本号，如 "Update sonnetaw v1.1"

---

需要帮助？查看 [DEPLOYMENT.md](DEPLOYMENT.md) 或提交 Issue。
